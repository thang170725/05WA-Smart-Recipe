# host 
BASE_URL=
VITE_BASE_URL=

# DB
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
DATABASE_URL=

# frontend
GOOGLE_CLIENT_ID= # dùng để login bằng google
EMAIL= # email gửi cho user, phục vụ chức năng quên mật khẩu hoặc gửi thông báo về email user
PASSWORD= # mật khẩu của email

VITE_EXERCISE_CATEGORIES=

# backend
LOG_LEVEL= # logging

# ===========================
# ========= AI AGENT ========
# ===========================
# LANGCHAIN KEY
LANGCHAIN_TRACING_V2=
LANGCHAIN_ENDPOINT=
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=

OLLAMA_NUM_PARALLEL=1 # biến môi trường tối ưu ollama (llm local) Cho phép đẩy tối đa các layer có thể vào VRAM

EMBEDDING_PROVIDER= # chỉ định xem muốn dùng gemini hay local
GOOGLE_API_KEY=
MODEL_EMBEDDING_LOCAL_NAME="" # tên model local embedding
# Ép embedding CPU — tránh tranh VRAM với Ollama (GPU ~4GB)
EMBEDDING_DEVICE=
LLM_OPTION= # local = ollama qwen2.5:7b | key = gemini
LOCAL_LLM_MODEL=
OLLAMA_NUM_CTX=
LLM_AUTO_FALLBACK=

# Ngưỡng cosine similarity tối thiểu để giữ tool (0–1)
DEFAULT_RAG_SCORE_THRESHOLD=

# ---- Loop limits ----
DEFAULT_MAX_ITERATIONS=
DEFAULT_MAX_RETRIEVAL_RETRIES=
DEFAULT_MAX_EXECUTION_STEPS=

# Số tool tối đa lấy từ Vector DB mỗi lượt hỏi
DEFAULT_TOOL_TOP_K=